# 2026-06-04 Memory Log

## Summary of Work Done
- Fully resolved remaining dashboard run creation and prefill gaps:
  - Centralized run creation logic by removing the old `New Run` creation modal and state from `apps/web/app/(app)/dashboard/DashboardClient.tsx`.
  - Refactored all "New Run" buttons on Dashboard to route directly to `/live?focus=prompt` or `/live?prefill_prompt_from=<debate_id>`.
  - Standardized URL query parameter parsing in `/live` (`page.tsx`) to pull prefill prompts directly from the `/api/debates/<id>` endpoint via the client, supporting both authenticated users and public share runs.
- Resolved Auth Success Redirects:
  - Configured successful Login and Registration flows to redirect users directly to the Arena at `/live` instead of the Overview page.
- Built a Production-Grade Provider-Agnostic Inference Gateway (`model_gateway`):
  - Created structured schemas (`types.py`) for requests and responses.
  - Set up tier-based model pools (`pools.py`) mapping free vs. premium models.
  - Implemented credit and cost safety checks (`costs.py`) to prevent run credit overrun.
  - Developed a unified routing policy engine (`policy.py`) supporting smart routing, direct routing, and high-availability fallback.
  - Created provider adapters (`adapters.py`) wrapping LiteLLM `acompletion` calls with custom retry and routing definitions.
  - Integrated the gateway package into agent execution (`agents.py`) and debate creation endpoint (`routes/debates.py`).
  - Added new metadata columns to `LLMUsageLog` model schema to log gateway policy execution, fallback status, costs, and retry metrics.
- Verification and Quality Assurance:
  - Wrote comprehensive backend unit tests for the model gateway under `apps/api/tests/test_model_gateway.py`.
  - Verified and successfully passed all model gateway tests under pytest.
  - Successfully built the Next.js production build (`npm run build` in `apps/web` exited with 0).
