# Daily Log — 2026-06-09

## Patchset: Final Trust, Search, Guard Wiring & Test Greening

### Phase 1 — LLM Guard Wiring (7 routes)
- `routes/oracle.py`: POST /oracle + POST /oracle/{id}/fork now call `require_llm_action_allowed` with `oracle_session` and `oracle_fork` actions
- `routes/redteam.py`: POST /redteam calls guard with `redteam_session` action
- `routes/challenge.py`: POST /challenge/{id}/round calls guard with `challenge_round` action + `debate_id`
- `routes/voting.py`: POST /voting/{id}/predict calls guard with `voting_prediction` action + `debate_id`
- `routes/voting.py`: GET /voting/{id}/reveal upgraded from `get_optional_user` to `get_current_user` + guard before LLM extraction
- `routes/arena.py`: GET /arena/{id}/divergence upgraded from `get_optional_user` to `get_current_user` + guard before `_execute_divergence_computation`
- Created `tests/test_llm_guard_routes.py` with 7 route-level guard tests

### Phase 2 — Search Fix
- `SmartSearch.tsx`: Expanded SearchItem type to include `mode`, `status`, `title`, `createdAt`; searches all fields via `getSearchableText()` helper; added `aria-live` result count, Escape clears query
- `RunsPageClient.tsx`: Maps full data (id, prompt, mode, status, createdAt) to SmartSearch
- `runs-table.tsx`: Includes `mode` in searchable text; fixed `auth: false` bug to `auth: true`
- `routes/debates.py`: Backend `GET /debates` now searches `id`, `prompt`, `mode`, `status` via `sa.or_()`
- Added `runs.smartSearch.hint` translation key in en.json and tr.json

### Phase 3 — DivergenceMeter Tests
- Root cause: `vi.resetAllMocks()` was resetting `window.matchMedia` mock from vitest.setup.ts
- Fix: Changed to `vi.clearAllMocks()` + added `ensureMatchMedia()` helper
- All 5 DivergenceMeter tests now pass (was 3 failing)

### Verification
- Backend: 34/34 tests pass
- Frontend: 43/43 tests pass (8 test files, 0 failures)
- TypeScript: clean
- Lint: clean (our files)
- Build: passes (50 routes)
- Pushed commit `859fb56` to main

### Files Changed
- Modified: oracle.py, redteam.py, challenge.py, voting.py, arena.py, debates.py
- New: tests/test_llm_guard_routes.py
- Modified: SmartSearch.tsx, RunsPageClient.tsx, runs-table.tsx
- Modified: en.json, tr.json
- Modified: DivergenceMeter.test.tsx
- Modified: tests/test_voting_access.py
